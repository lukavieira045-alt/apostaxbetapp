# Reserved workflow template for Evolution AI APK builds.
# This repository is intentionally NOT used by the Evolution AI build pipeline.
# APK builds must use a dedicated Evolution AI repository so ApostaXBet is never modified.
name: Evolution AI APK (reserved)

on:
  workflow_dispatch:

jobs:
  reserved:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Reserved only - no Evolution AI files are built in ApostaXBet."
